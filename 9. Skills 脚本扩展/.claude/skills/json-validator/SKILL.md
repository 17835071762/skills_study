---
name: json-validator
description: 根据 JSON Schema 验证 JSON 数据文件的格式。当用户需要验证 JSON 数据、检查 JSON 格式、或使用 Schema 验证数据时使用。
---

# JSON Schema 验证技能

这个技能帮助你根据 JSON Schema 验证 JSON 数据文件的格式。

## 可用脚本

- **`scripts/validate_schema.py`** — 根据 Schema 验证 JSON 数据

## 前置依赖

需要安装 jsonschema 库：

```bash
pip install jsonschema